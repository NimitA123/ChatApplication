<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"

    android:background="@drawable/nimit"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycleView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"></androidx.recyclerview.widget.RecyclerView>

    <EditText
        android:id="@+id/sendMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="12dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="12dp"
        android:background="@drawable/rectangle"
        android:hint="Please Enter the the Message"
        android:padding="10dp"
        android:textColor="@color/black" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="-50dp"
        android:layout_marginTop="12dp"
        android:id = "@+id/ic_sendMsg"
        android:background="@color/black"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="23dp"
        android:layout_toEndOf="@id/sendMessage"
        android:src="@drawable/ic_send" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding = "10dp"
        android:id = "@+id/selectOption"
        android:textColor="@color/black"
        android:background="@drawable/rectangle"
        android:layout_margin="12dp"
        android:layout_above="@id/sendMessage"
        android:hint = "Please types 0 and 1 "
        ></EditText>

</RelativeLayout>